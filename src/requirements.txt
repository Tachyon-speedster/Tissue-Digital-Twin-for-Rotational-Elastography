# Tissue Digital Twin v2 — Python Dependencies
# =============================================
# Install with: pip install -r requirements.txt
#
# Ubuntu/Linux system packages:
#   sudo apt install python3-tk libsdl2-dev libsdl2-image-dev libsdl2-ttf-dev
#   sudo apt install v4l2loopback-dkms   (for DroidCam/Iriun virtual devices)

numpy>=1.24.0
pygame>=2.4.0
matplotlib>=3.7.0
opencv-python>=4.8.0
scipy>=1.11.0
pandas>=2.0.0
